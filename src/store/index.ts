import Vue from "vue";
import Vuex from "vuex";

import Darkmode from "@/store/darkmode";

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: { Darkmode },
});
